name: Couple

on:
  workflow_dispatch:
    inputs:
      novio:
        description: 'Nombre del Novio'
        required: true
        type: string
      novia:
        description: 'Nombre del Novio'
        required: true
        type: string

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Mensaje de Amor
        run: |
          echo "${{ inputs.novio}} y ${{ inputs.novia }}!"
          echo "♡ ♡ ♡${{ inputs.novia }} es mi novia y la quiero mucho ♡ ♡ ♡"
